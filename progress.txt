## Iteration 4: AI Waiter Enhancement — Progress Log

### Context
- Enhancing the AI waiter chatbot with Rainbow AI integration and UX improvements
- Rainbow AI project at: C:\Users\Jyue\Documents\1-projects\Software Projects\rainbow-ai
- Two codebases involved: fnb-online-ordering (Next.js) and rainbow-ai (Express)

### Completed Stories (all 10 passing)

**US-401** — Populated Makan Moments KB in Rainbow AI
- Updated profiles.json kbDir from absolute Windows path to relative `.rainbow-kb-makan`
- Wrote cafe-facts.md, faq.md, ordering.md, soul.md with proper cafe content
- Updated data-makan/settings.json: cafe AI waiter system prompt, removed hostel phone numbers

**US-402** — Added GET /api/chat/:profileId/kb-context to Rainbow AI webchat router
- Returns compiled systemPrompt + kbFiles list, 5-min in-memory cache

**US-403** — Wired fnb system prompt to Rainbow AI KB
- Added RAINBOW_AI_ENABLED and RAINBOW_AI_URL env vars
- fetchRainbowKBContext() with 3s timeout, graceful fallback to local files
- invalidateSystemPromptCache() also clears Rainbow cache

**US-404** — Added time-aware proactive menu suggestions
- buildTimeContext() computes Malaysia time fresh per call (not cached)
- Includes current time, day, and serving categories from time-slots config
- Injected into system prompt before knowledge block

**US-405** — Added quick-reply buttons in chat
- Created src/components/chat/quick-replies.tsx
- 3 context sets: initial, ordering, menu — keyword-based heuristic
- Pill-shaped, horizontally scrollable, 44px touch targets
- Hidden when typing or loading

**US-406** — Rendered food photo cards when AI mentions menu items
- Created /api/menu endpoint (lightweight: id, code, name, price)
- Created src/components/chat/menu-item-card.tsx with Add button
- chat-bubble.tsx detects item codes via regex, renders max 3 cards
- Cards show thumbnail, name, price, +1 animation on add

**US-407** — Added thumbs up/down feedback on AI messages
- Feedback icons on assistant messages (hover on desktop, always on mobile)
- POST /api/chat/feedback → chat_feedback table (CREATE TABLE IF NOT EXISTS)
- "Thanks!" toast fades after 2s, one vote per message

**US-408** — Added conversation logging to Rainbow AI
- POST /api/chat/log proxies to Rainbow AI's webchat message endpoint
- Only logs if RAINBOW_AI_ENABLED=true, 5s timeout, fire-and-forget

**US-409** — Added Chat Analytics tab to admin dashboard
- Created ChatAnalyticsPanel with summary cards, satisfaction bar, recent table
- GET /api/admin/chat-analytics (auth-gated) queries chat_feedback table
- Lazy-loaded via next/dynamic, registered in admin tabs

**US-410** — Added conversation session persistence
- Session ID stored in localStorage (mm_chat_session_id) with 24h expiry
- "new chat" / "start over" commands clear session
- Session ID passed to feedback API for correlation

### Key patterns from Iteration 4
- verifyAdminToken(token) takes a string, not a Request — use checkAuth() pattern from settings route
- Rainbow AI KB endpoint caches in-memory with simple Map + TTL
- Chat bubble components need `"use client"` for useState/useCallback hooks
- Build may OOM with 497 static pages — use NODE_OPTIONS="--max-old-space-size=8192"
- `animate-in fade-in` Tailwind classes work for simple enter animations

### Key patterns from Iteration 3
- Optional SiteSettings fields should use `delete` to remove when cleared
- Admin settings endpoint uses generic `{ ...current, ...body }` merge — new fields auto-saved
- Use `next/dynamic` with `ssr: false` for admin components in customer-facing pages
- `useDeferredValue` is better than `useTransition` for immediate UI + deferred computation
- SW offline fallback must be static HTML in `public/`, not a Next.js route

## Pre-analysis for US-401 (Gemini 2.5 Pro)
This is not a Frappe/Python story; it's a file-based content and configuration update for the "Rainbow AI" Node.js project.

**Project:** Rainbow AI
**Objective:** Populate the Makan Moments cafe profile with knowledge and correct its configuration.

**Files to Modify:**
*   `rainbow-ai/profiles.json`
*   `rainbow-ai/.rainbow-kb-makan/cafe-facts.md`
*   `rainbow-ai/.rainbow-kb-makan/faq.md`
*   `rainbow-ai/.rainbow-kb-makan/ordering.md` (likely needs creation)
*   `rainbow-ai/.rainbow-kb-makan/soul.md`
*   `rainbow-ai/data-makan/settings.json`

**Implementation Approach:**
1.  **Update `profiles.json`:** Change the `kbDir` for the `makan-moments` profile to the relative path: `.rainbow-kb-makan`.
2.  **Copy Knowledge:** Read content from `fnb-online-ordering/knowledge/cafe-facts.md` and `fnb-online-ordering/knowledge/faq.md`.
3.  **Populate KB Files:**
    *   Write the copied content into `.rainbow-kb-makan/cafe-facts.md` and `.rainbow-kb-makan/faq.md`.
    *   Create and write the pre-order flow description into `.rainbow-kb-makan/ordering.md`.
    *   Update `.rainbow-kb-makan/soul.md` with the specified personality traits.
4.  **Update Settings:** Modify `data-makan/settings.json` to set the `system_prompt` to a cafe AI waiter persona and remove old staff phone numbers.

**Gotchas:**
*   This is a file I/O task, not a Frappe framework task. No DocTypes or hooks are involved.
*   Ensure the `kbDir` path is relative, exactly as specified, to support both Windows and Linux.
*   Do not modify `.rainbow-kb-makan/AGENTS.md`.
*   The `system_prompt` should define the AI's personality, not list menu items.


## Pre-analysis for US-401 (Gemini 2.5 Pro)
Technical Brief for US-401: Populate Makan Moments AI Knowledge

This story involves updating configuration and knowledge base files for the "Makan Moments" profile within the separate Rainbow AI project. It is a file-based task, not a Frappe/Python modification.

**1. Primary Files to Modify (in `rainbow-ai` project):**
*   `profiles.json`: To update the knowledge base path.
*   `.rainbow-kb-makan/cafe-facts.md`: To be populated with cafe details.
*   `.rainbow-kb-makan/faq.md`: To be populated with FAQs.
*   `.rainbow-kb-makan/ordering.md`: **(New file)** To explain the ordering process.
*   `.rainbow-kb-makan/soul.md`: To define the AI's personality.
*   `data-makan/settings.json`: To set the system prompt and update staff contacts.

**2. Information Source Files (in `fnb-online-ordering` project):**
*   `knowledge/cafe-facts.md`: Source of truth for cafe business details.
*   `knowledge/faq.md`: Source of truth for frequently asked questions.

**3. Implementation Approach:**
1.  Read the contents of `knowledge/cafe-facts.md` and `knowledge/faq.md` from the `fnb-online-ordering` directory.
2.  Modify `rainbow-ai/profiles.json`, changing the `kbDir` for the `makan-moments` profile to the relative path: `.rainbow-kb-makan`.
3.  Populate `rainbow-ai/.rainbow-kb-makan/` with content adapted from the source files and story description.
4.  Update `rainbow-ai/data-makan/settings.json` to replace the `system_prompt` with a cafe AI waiter identity and remove placeholder staff phone numbers.

**4. Edge Cases & Gotchas:**
*   **Cross-Project Paths:** The task requires reading from the `fnb-online-ordering` project and writing to the `rainbow-ai` project.
*   **Relative Path:** The `kbDir` value *must* be exactly `.rainbow-kb-makan` (a relative path with no slashes) to ensure it works on both Windows and Linux.
*   **Content Creation:** The `ordering.md` file needs to be created from scratch based on the acceptance criteria.
*   **System Prompt:** The prompt in `settings.json` should establish a persona, not list menu items or other dynamic knowledge.


## Pre-analysis for US-401 (Gemini 2.5 Pro)
Here is the technical brief for user story US-401.

**Subject:** Technical Brief for US-401: Populate Makan Moments KB in Rainbow AI

**Overview:** This task involves populating the `makan-moments` profile in the Rainbow AI project with knowledge and configuration for a cafe. The work is limited to creating and editing Markdown and JSON files. No Frappe DocTypes or Python code are involved.

**File Targets:**
*   `rainbow-ai/profiles.json`
*   `rainbow-ai/data-makan/settings.json`
*   `rainbow-ai/.rainbow-kb-makan/cafe-facts.md`
*   `rainbow-ai/.rainbow-kb-makan/faq.md`
*   `rainbow-ai/.rainbow-kb-makan/ordering.md`
*   `rainbow-ai/.rainbow-kb-makan/soul.md`

**Implementation Plan:**
1.  Modify `profiles.json` to set the `kbDir` for `makan-moments` to the relative path `.rainbow-kb-makan`.
2.  Create/replace `.rainbow-kb-makan/cafe-facts.md` and `.rainbow-kb-makan/faq.md` by copying content from the source-of-truth files located in `fnb-online-ordering/knowledge/`.
3.  Create `.rainbow-kb-makan/ordering.md` describing the customer's pre-order process.
4.  In `data-makan/settings.json`, rewrite the `system_prompt` to give the AI the persona of a cafe waiter, and remove placeholder staff phone numbers.
5.  Update `soul.md` with personality traits for a proactive, multilingual food enthusiast.

**Gotchas & Edge Cases:**
*   **Relative Paths:** Ensure the `kbDir` path is relative to function across different operating systems (dev vs. production).
*   **Dynamic Knowledge:** The `system_prompt` should define the AI's role and personality but must NOT include menu items, as they are loaded dynamically from the knowledge base.
*   **File Integrity:** Do not alter the existing `AGENTS.md` file within the KB directory.

