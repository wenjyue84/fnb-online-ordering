## Iteration 5: Core Ordering Flow — Progress Log

### Context
- Reducing customer wait time by streamlining the order-to-kitchen pipeline
- depositRequired already exists in SiteSettings type (default: false) and admin settings toggle — just needs wiring into approve action
- web-push already installed; VAPID keys already in .env.local; push_subscriptions table exists for admins
- public/sw.js already has push + notificationclick handlers
- /kitchen route does not yet exist

### Pre-analysis (research phase)

**SSE in Next.js App Router (for US-504):**
- Use `export const runtime = 'edge'` — avoids Vercel's 60s Node.js serverless limit (Edge gets 300s)
- Must include `X-Accel-Buffering: no` header — prevents NGINX/Vercel proxy buffering events
- Edge Runtime cannot use Pool from @neondatabase/serverless — use `neon()` tagged template directly
- JWT verification in Edge: use `jose` jwtVerify() directly (not verifyAdminToken() which uses Node.js APIs)
- EventSource sends cookies automatically on same-origin — admin JWT cookie auth works
- SSE wire format: `data: {json}\n\n` for events, `: ping\n\n` for heartbeat comments
- Client reconnect: EventSource has built-in reconnect; also implement manual attempt counter for fallback to polling

**Customer Web Push (for US-506):**
- Separate table `order_push_subscriptions` (order_id, endpoint, p256dh, auth) — CREATE TABLE IF NOT EXISTS in route
- iOS only delivers push to PWA added to Home Screen (iOS 16.4+) — gate subscribe button behind: /iPad|iPhone|iPod/.test(navigator.userAgent) && !window.matchMedia('(display-mode: standalone)').matches
- Use urlBase64ToUint8Array() helper for applicationServerKey conversion (10-line utility, inline it)
- 410 Gone from push service = expired subscription → DELETE that row (same pattern as admin push in src/app/api/orders/route.ts)
- NEXT_PUBLIC_VAPID_PUBLIC_KEY already in .env.example — reuse existing VAPID setup

### Completed Stories

**US-501** — Wire depositRequired into approve action
- In approve action: getSiteSettings().depositRequired -> if false sets status=preparing; if true keeps approved
- GET /api/settings now exposes depositRequired field
- Customer /order/[id] page: hides T&G payment section when !depositRequired && status===preparing

**US-502** — Kitchen PIN auth (/kds/login)
- POST /api/kds/login: reads kitchenPin from SiteSettings, issues kitchen_token JWT (role: kds, 12h)
- verifyKdsToken() + verifyKitchenToken alias added to auth.ts
- kitchenPin field added to SiteSettings with default 1234
- Admin Settings panel: Kitchen PIN input field added

**US-503** — Kitchen Display System (KDS) — /kds page
- /kds: tablet-optimised, shows preparing + approved orders, polls every 5s
- GET /api/kds/orders: returns approved+preparing orders (kds-auth-gated)
- PATCH /api/kds/orders/[id]: action=start->preparing, action=ready->ready
- Color-coded urgency: red <=5min, yellow 6-10min, green >10min, grey no ETA

**US-504** — Admin SSE for real-time new order notifications
- GET /api/admin/orders/stream: Edge Runtime, JWT cookie auth via jose, polls every 3s, heartbeat every 20s
- admin-orders-bell.tsx: EventSource on mount, 3-attempt reconnect with 5s delay, fallback to 30s polling
- Status indicator: green Live / grey Polling / yellow Connecting dot in bell and dropdown header
- On new_order event: immediate fetchOrders() + optimistic badge increment via synthetic order prepend
- Cleanup on unmount (EventSource.close() + clearTimeout + clearInterval)

**US-505** — Bulk approve with smart estimated-ready time
- POST /api/admin/orders/bulk-approve: approves all pending_approval, calculates ready time per order
- calculateSmartReadyTime(items): max(10, min(30, 10 + 2 * items.length)) minutes
- AdminOrdersPanel: Approve All Pending (N) button in Pending tab with confirmation modal
- AdminOrderCard ApproveModal: prefilled with calculateSmartReadyTime suggestion

**US-506** — Customer Web Push on order ready
- POST /api/orders/[id]/push-subscribe: stores subscription in order_push_subscriptions (CREATE TABLE IF NOT EXISTS)
- Admin orders PATCH mark_ready: sendCustomerPush() fire-and-forget after status update
- PushSubscribeButton: iOS without PWA shows fallback text; on permission grant, subscribes + POSTs
- sw.js notificationclick: uses event.notification.data?.url - correct for admin + customer push

**US-507** — Improved order status page UX
- Expiry countdown for pending_approval: 1s tick, red when <5min
- Last updated X sec ago: secsSince counter resets on each successful poll
- Confetti on ready: CSS keyframes overlay, 21 emoji spans, 3s, on status transition to ready
- WhatsApp pre-filled CTA in rejected + expired (NEXT_PUBLIC_WHATSAPP_NUMBER env var)
- Copy order ID button on terminal statuses (ready/rejected/expired)
- i18n keys added to all 3 locale files: lastUpdatedAgo, expiryCountdown, expiredWhatsappMsg, whatsappReorder, copyOrderId, copied

### Key patterns from Iteration 5

- Edge Runtime SSE: use neon() directly (not sql from @/lib/db - Pool is not Edge-compatible)
- Edge JWT auth: parse cookie manually with regex, use jose jwtVerify()
- SSE format: event: new_order
data: {json}

 for named events, : ping

 for heartbeat
- EventSource onerror fires immediately on refused connection - use attempt counter for fallback to polling
- Build OOM workaround: NODE_OPTIONS=--max-old-space-size=16384
- Uint8Array cast for pushManager: urlBase64ToUint8Array() as unknown as ArrayBuffer
- web-push in admin route: setVapidDetails() at module level, fire-and-forget with 410 Gone cleanup

---

## Iteration 6: Pre-Order Flow Enhancement — Progress Log

### Context
- POS dashboard, POS mode toggle, customer UX enhancements, alarm system, PWA install
- US-601 through US-507 from iter 5 already done; iter 6 adds US-601-US-612

### Completed Stories

**US-601** — Built-in Simple POS Dashboard (/pos)
- /pos page: kanban 3-column layout (Pending/Preparing/Ready) with order cards
- PosCard shows: order ID (large), items (max 3 + overflow), total, contact, ETA, elapsed time
- One-tap approve/reject/mark-ready/confirm-payment on cards
- Order detail modal with full info, payment screenshot, print receipt (window.print + @media print)
- Daily summary footer: today's order count, revenue, avg order value
- Auto-refresh every 10 seconds
- Responsive: 3-column desktop, stacked tabs on mobile
- Admin JWT auth — redirects to /admin/login on 401
- Admin sidebar has POS tab linking to /pos (opens new tab)
- Back button from /pos → /admin/orders

**US-602** — POS Mode Toggle — Built-in vs FeedMe Manual Entry
- posMode: 'builtin' | 'feedme_manual' in SiteSettings (default: feedme_manual)
- Admin Settings > Pre-Order tab: radio group for POS Mode
- GET /api/settings exposes posMode publicly
- Admin order card: yellow FeedMe banner + checkbox on preparing cards when posMode=feedme_manual
- KDS page: FeedMe banner + checkbox on preparing cards, gates Mark Ready button
- POS page: FeedMe banner + checkbox on preparing cards, gates Mark Ready button
- PATCH /api/admin/orders/[id] action=feedme_entered: sets feedme_entered=TRUE
- PATCH /api/kds/orders/[id] action=feedme_entered: sets feedme_entered=TRUE (kitchen-auth-gated)
- GET /api/kds/orders now returns feedme_entered column
- feedme_entered column: BOOLEAN DEFAULT FALSE on tray_orders (migration in admin orders GET route)

### Key patterns from Iteration 6

- POS page uses direct fetch() instead of useAdminOrders() hook for more control over auth redirect
- FeedMe entered checkbox handles its own API call inline (not through parent action handlers)
- posMode fetched once from /api/settings on mount, not re-fetched on refresh

---

## Iteration 4: AI Waiter Enhancement — Progress Log

### Context
- Enhancing the AI waiter chatbot with Rainbow AI integration and UX improvements
- Rainbow AI project at: C:\Users\Jyue\Documents\1-projects\Software Projects\rainbow-ai
- Two codebases involved: fnb-online-ordering (Next.js) and rainbow-ai (Express)

### Completed Stories (all 10 passing)

**US-401** — Populated Makan Moments KB in Rainbow AI
- Updated profiles.json kbDir from absolute Windows path to relative `.rainbow-kb-makan`
- Wrote cafe-facts.md, faq.md, ordering.md, soul.md with proper cafe content
- Updated data-makan/settings.json: cafe AI waiter system prompt, removed hostel phone numbers

**US-402** — Added GET /api/chat/:profileId/kb-context to Rainbow AI webchat router
- Returns compiled systemPrompt + kbFiles list, 5-min in-memory cache

**US-403** — Wired fnb system prompt to Rainbow AI KB
- Added RAINBOW_AI_ENABLED and RAINBOW_AI_URL env vars
- fetchRainbowKBContext() with 3s timeout, graceful fallback to local files
- invalidateSystemPromptCache() also clears Rainbow cache

**US-404** — Added time-aware proactive menu suggestions
- buildTimeContext() computes Malaysia time fresh per call (not cached)
- Includes current time, day, and serving categories from time-slots config
- Injected into system prompt before knowledge block

**US-405** — Added quick-reply buttons in chat
- Created src/components/chat/quick-replies.tsx
- 3 context sets: initial, ordering, menu — keyword-based heuristic
- Pill-shaped, horizontally scrollable, 44px touch targets
- Hidden when typing or loading

**US-406** — Rendered food photo cards when AI mentions menu items
- Created /api/menu endpoint (lightweight: id, code, name, price)
- Created src/components/chat/menu-item-card.tsx with Add button
- chat-bubble.tsx detects item codes via regex, renders max 3 cards
- Cards show thumbnail, name, price, +1 animation on add

**US-407** — Added thumbs up/down feedback on AI messages
- Feedback icons on assistant messages (hover on desktop, always on mobile)
- POST /api/chat/feedback → chat_feedback table (CREATE TABLE IF NOT EXISTS)
- "Thanks!" toast fades after 2s, one vote per message

**US-408** — Added conversation logging to Rainbow AI
- POST /api/chat/log proxies to Rainbow AI's webchat message endpoint
- Only logs if RAINBOW_AI_ENABLED=true, 5s timeout, fire-and-forget

**US-409** — Added Chat Analytics tab to admin dashboard
- Created ChatAnalyticsPanel with summary cards, satisfaction bar, recent table
- GET /api/admin/chat-analytics (auth-gated) queries chat_feedback table
- Lazy-loaded via next/dynamic, registered in admin tabs

**US-410** — Added conversation session persistence
- Session ID stored in localStorage (mm_chat_session_id) with 24h expiry
- "new chat" / "start over" commands clear session
- Session ID passed to feedback API for correlation

### Key patterns from Iteration 4
- verifyAdminToken(token) takes a string, not a Request — use checkAuth() pattern from settings route
- Rainbow AI KB endpoint caches in-memory with simple Map + TTL
- Chat bubble components need `"use client"` for useState/useCallback hooks
- Build may OOM with 497 static pages — use NODE_OPTIONS="--max-old-space-size=8192"
- `animate-in fade-in` Tailwind classes work for simple enter animations

### Key patterns from Iteration 3
- Optional SiteSettings fields should use `delete` to remove when cleared
- Admin settings endpoint uses generic `{ ...current, ...body }` merge — new fields auto-saved
- Use `next/dynamic` with `ssr: false` for admin components in customer-facing pages
- `useDeferredValue` is better than `useTransition` for immediate UI + deferred computation
- SW offline fallback must be static HTML in `public/`, not a Next.js route

## Pre-analysis for US-401 (Gemini 2.5 Pro)
This is not a Frappe/Python story; it's a file-based content and configuration update for the "Rainbow AI" Node.js project.

**Project:** Rainbow AI
**Objective:** Populate the Makan Moments cafe profile with knowledge and correct its configuration.

**Files to Modify:**
*   `rainbow-ai/profiles.json`
*   `rainbow-ai/.rainbow-kb-makan/cafe-facts.md`
*   `rainbow-ai/.rainbow-kb-makan/faq.md`
*   `rainbow-ai/.rainbow-kb-makan/ordering.md` (likely needs creation)
*   `rainbow-ai/.rainbow-kb-makan/soul.md`
*   `rainbow-ai/data-makan/settings.json`

**Implementation Approach:**
1.  **Update `profiles.json`:** Change the `kbDir` for the `makan-moments` profile to the relative path: `.rainbow-kb-makan`.
2.  **Copy Knowledge:** Read content from `fnb-online-ordering/knowledge/cafe-facts.md` and `fnb-online-ordering/knowledge/faq.md`.
3.  **Populate KB Files:**
    *   Write the copied content into `.rainbow-kb-makan/cafe-facts.md` and `.rainbow-kb-makan/faq.md`.
    *   Create and write the pre-order flow description into `.rainbow-kb-makan/ordering.md`.
    *   Update `.rainbow-kb-makan/soul.md` with the specified personality traits.
4.  **Update Settings:** Modify `data-makan/settings.json` to set the `system_prompt` to a cafe AI waiter persona and remove old staff phone numbers.

**Gotchas:**
*   This is a file I/O task, not a Frappe framework task. No DocTypes or hooks are involved.
*   Ensure the `kbDir` path is relative, exactly as specified, to support both Windows and Linux.
*   Do not modify `.rainbow-kb-makan/AGENTS.md`.
*   The `system_prompt` should define the AI's personality, not list menu items.


## Pre-analysis for US-401 (Gemini 2.5 Pro)
Technical Brief for US-401: Populate Makan Moments AI Knowledge

This story involves updating configuration and knowledge base files for the "Makan Moments" profile within the separate Rainbow AI project. It is a file-based task, not a Frappe/Python modification.

**1. Primary Files to Modify (in `rainbow-ai` project):**
*   `profiles.json`: To update the knowledge base path.
*   `.rainbow-kb-makan/cafe-facts.md`: To be populated with cafe details.
*   `.rainbow-kb-makan/faq.md`: To be populated with FAQs.
*   `.rainbow-kb-makan/ordering.md`: **(New file)** To explain the ordering process.
*   `.rainbow-kb-makan/soul.md`: To define the AI's personality.
*   `data-makan/settings.json`: To set the system prompt and update staff contacts.

**2. Information Source Files (in `fnb-online-ordering` project):**
*   `knowledge/cafe-facts.md`: Source of truth for cafe business details.
*   `knowledge/faq.md`: Source of truth for frequently asked questions.

**3. Implementation Approach:**
1.  Read the contents of `knowledge/cafe-facts.md` and `knowledge/faq.md` from the `fnb-online-ordering` directory.
2.  Modify `rainbow-ai/profiles.json`, changing the `kbDir` for the `makan-moments` profile to the relative path: `.rainbow-kb-makan`.
3.  Populate `rainbow-ai/.rainbow-kb-makan/` with content adapted from the source files and story description.
4.  Update `rainbow-ai/data-makan/settings.json` to replace the `system_prompt` with a cafe AI waiter identity and remove placeholder staff phone numbers.

**4. Edge Cases & Gotchas:**
*   **Cross-Project Paths:** The task requires reading from the `fnb-online-ordering` project and writing to the `rainbow-ai` project.
*   **Relative Path:** The `kbDir` value *must* be exactly `.rainbow-kb-makan` (a relative path with no slashes) to ensure it works on both Windows and Linux.
*   **Content Creation:** The `ordering.md` file needs to be created from scratch based on the acceptance criteria.
*   **System Prompt:** The prompt in `settings.json` should establish a persona, not list menu items or other dynamic knowledge.


## Pre-analysis for US-401 (Gemini 2.5 Pro)
Here is the technical brief for user story US-401.

**Subject:** Technical Brief for US-401: Populate Makan Moments KB in Rainbow AI

**Overview:** This task involves populating the `makan-moments` profile in the Rainbow AI project with knowledge and configuration for a cafe. The work is limited to creating and editing Markdown and JSON files. No Frappe DocTypes or Python code are involved.

**File Targets:**
*   `rainbow-ai/profiles.json`
*   `rainbow-ai/data-makan/settings.json`
*   `rainbow-ai/.rainbow-kb-makan/cafe-facts.md`
*   `rainbow-ai/.rainbow-kb-makan/faq.md`
*   `rainbow-ai/.rainbow-kb-makan/ordering.md`
*   `rainbow-ai/.rainbow-kb-makan/soul.md`

**Implementation Plan:**
1.  Modify `profiles.json` to set the `kbDir` for `makan-moments` to the relative path `.rainbow-kb-makan`.
2.  Create/replace `.rainbow-kb-makan/cafe-facts.md` and `.rainbow-kb-makan/faq.md` by copying content from the source-of-truth files located in `fnb-online-ordering/knowledge/`.
3.  Create `.rainbow-kb-makan/ordering.md` describing the customer's pre-order process.
4.  In `data-makan/settings.json`, rewrite the `system_prompt` to give the AI the persona of a cafe waiter, and remove placeholder staff phone numbers.
5.  Update `soul.md` with personality traits for a proactive, multilingual food enthusiast.

**Gotchas & Edge Cases:**
*   **Relative Paths:** Ensure the `kbDir` path is relative to function across different operating systems (dev vs. production).
*   **Dynamic Knowledge:** The `system_prompt` should define the AI's role and personality but must NOT include menu items, as they are loaded dynamically from the knowledge base.
*   **File Integrity:** Do not alter the existing `AGENTS.md` file within the KB directory.

## Iteration 1 - Thu, Mar 12, 2026  8:15:57 PM
Completed: Populate Makan Moments KB and config in Rainbow AI (ID: US-401) in 19m


## Pre-analysis for US-501 (Gemini 2.5 Pro)
This user story (US-501) describes a **Next.js/TypeScript** implementation, not a Frappe/Python one. The technical brief below is based on the actual file paths and logic in the story.

**Technical Brief: US-501**

*   **Files to Modify:**
    *   `src/app/api/admin/orders/[id]/route.ts`: To implement the core conditional logic.
    *   `src/app/api/settings/route.ts`: To expose the setting to the client.
    *   `src/app/[locale]/order/[id]/page.tsx`: The customer-facing UI that conditionally renders components.
    *   `src/lib/site-settings.ts`: (Verify) The function that reads the setting.

*   **Key Data/API Endpoints (Analogous to DocTypes/Hooks):**
    *   **Data Source:** `data/site-settings.json` (contains the `depositRequired` flag).
    *   **API Endpoint:** `PATCH /api/admin/orders/[id]`: The whitelisted API route for the `approve` action.
    *   **API Endpoint:** `GET /api/settings`: The public endpoint for client-side settings.

*   **Implementation Approach:**
    1.  In `.../api/admin/orders/[id]/route.ts`, modify the `PATCH` handler. Inside the `approve` action block, call `getSiteSettings()`.
    2.  Branch the logic: if `depositRequired` is `false`, set the order status to `'preparing'`. Otherwise, keep the existing `'approved'` status logic.
    3.  In `.../api/settings/route.ts`, add the `depositRequired` field to the public response payload.
    4.  In `.../order/[id]/page.tsx`, fetch settings from `/api/settings` and use the flag to hide the payment section, showing a "preparing" message instead when appropriate.

*   **Gotchas to Watch For:**
    *   Ensure the `estimatedReady` time is saved correctly in both branches of the logic.
    *   The client page (`page.tsx`) must handle the loading state for the new settings API call to prevent UI flicker or race conditions.
    *   Confirm `getSiteSettings()` gracefully handles a missing `depositRequired` key in the JSON, defaulting to `false`.


## Pre-analysis for US-501 (Gemini 2.5 Pro)
### Technical Brief: US-501 (Frappe/Python Adaptation)

This brief translates the provided user story's logic into a Frappe/Python context for the `lhdn_payroll_integration` app. The core task is to make a document's approval action conditional based on a global setting.

**1. Python Files to Modify:**
- `lhdn_payroll_integration/lhdn_payroll_integration/doctype/payroll_submission/payroll_submission.py`: To implement the conditional logic in the document's controller class.
- `lhdn_payroll_integration/lhdn_payroll_integration/api.py`: To create a new whitelisted API endpoint that exposes the required setting to the client-side.

**2. Frappe DocTypes & Hooks:**
- **`Payroll Submission`** (Assumed name): The primary transactional DocType being approved. It needs a `status` field supporting values like `'Approved'` and `'Preparing'`.
- **`LHDN Settings`** (Singleton): A new or existing singleton settings DocType must have a **Check** field named `deposit_required` (default: 0).
- **`hooks.py`**: May be needed to whitelist the new API endpoint in `api.py`.

**3. Recommended Implementation Approach:**
1.  In `payroll_submission.py`, define a whitelisted method like `approve_submission`.
2.  Inside this method, fetch the setting: `deposit_required = frappe.db.get_single_value('LHDN Settings', 'deposit_required')`.
3.  Branch the logic: `self.status = 'Approved' if deposit_required else 'Preparing'`.
4.  Save the document using `self.save()`.
5.  In `payroll_submission.js` (client script), use `frm.add_custom_button` to call the new method. On `refresh`, conditionally show/hide payment-related fields based on the document's status.

**4. Edge Cases & Gotchas:**
- **Workflows:** If a Frappe Workflow manages the status, place this logic in a Workflow Action script for the 'Approve' transition instead of a custom button to avoid bypassing the state machine.
- **Permissions:** Ensure the `approve_submission` method is restricted by role.
- **Caching:** Use `frappe.get_cached_doc` for settings to improve performance, but be aware of cache clearing if the setting changes often.


## Pre-analysis for US-501 (Gemini 2.5 Pro)
This user story is for a **Next.js (TypeScript/React)** application, not a Frappe/Python one. The file paths (`route.ts`, `page.tsx`) and technical notes reference a typical Next.js project structure.

Here is the technical brief based on the Next.js context provided:

**Project:** fnb-online-ordering (Next.js)
**User Story:** US-501

**1. Files to Modify:**
*   `src/app/api/admin/orders/[id]/route.ts`: To change the order status logic.
*   `src/app/api/settings/route.ts`: To expose the `depositRequired` setting publicly.
*   `src/app/[locale]/order/[id]/page.tsx`: The customer-facing UI that will conditionally render the payment section.
*   `src/lib/site-settings.ts`: Confirmed as the source for `getSiteSettings()`.

**2. Involved Framework Concepts:**
*   **API Routes:** The core logic resides in two Next.js API routes (`/api/admin/orders/[id]` and `/api/settings`).
*   **React Server/Client Components:** The order page (`page.tsx`) will fetch data and conditionally render UI based on the new setting.

**3. Implementation Approach:**
*   In the `approve` action of the admin orders API, call `getSiteSettings()`.
*   Implement a conditional: `if (settings.depositRequired)` then set status to `'approved'`, else set status to `'preparing'`.
*   Update the public settings API to include the `depositRequired` boolean in its response payload.
*   On the customer order page, fetch settings alongside order data. Use a React state or prop (`depositRequired`) to control the visibility of the payment upload section.

**4. Gotchas & Edge Cases:**
*   The `estimatedReady` time can be provided during the approve action and must be saved regardless of the `depositRequired` value.
*   The customer page logic must check both `!depositRequired` AND `order.status === 'preparing'` to show the correct confirmation message.
*   Ensure the public settings API only exposes safe, non-sensitive fields.


## Pre-analysis for US-501 (Gemini 2.5 Pro)
Here is the technical brief for the Claude agent.

**Technical Brief: US-501 - Implement `depositRequired` Logic**

This brief outlines the implementation of a feature where a system setting, `depositRequired`, alters the outcome of an order approval action within the `lhdn_payroll_integration` app.

**1. Python Files to Modify:**

*   **Primary:** `lhdn_payroll_integration/lhdn_payroll_integration/doctype/payroll_order/payroll_order.py` (assuming `Payroll Order` is the relevant DocType). The `approve_order` controller method will be created/modified here.
*   **Secondary:** `lhdn_payroll_integration/api.py`. A new whitelisted method `get_lhdn_settings` will be added to expose the setting to the client.
*   **Hooks:** `lhdn_payroll_integration/hooks.py` to whitelist the new API endpoints.

**2. Frappe DocTypes & Hooks:**

*   **DocType (Singleton):** `LHDN Settings`. A new field `deposit_required` (Type: Check, Default: 0) must be added.
*   **DocType:** `Payroll Order`. This DocType's status field will be conditionally updated.
*   **API Hook:** The `approve_order` method in `payroll_order.py` must be whitelisted (`@frappe.whitelist()`).

**3. Implementation Approach:**

1.  Modify the `LHDN Settings` DocType to include the `deposit_required` checkbox.
2.  In `payroll_order.py`, create the `approve_order(order_id)` method.
3.  Inside this method, fetch the setting: `settings = frappe.get_cached_doc('LHDN Settings')`.
4.  Conditionally set the order status:
    *   If `settings.deposit_required`: `order.status = 'Approved'`
    *   Else: `order.status = 'Preparing'`
5.  In `api.py`, create a `get_lhdn_settings` function to return `{'deposit_required': settings.deposit_required}`.

**4. Edge Cases & Gotchas:**

*   **Permissions:** Ensure `approve_order` validates user roles (e.g., 'Payroll Manager').
*   **Workflow:** If the `status` field is governed by a Frappe Workflow, you must use `frappe.model.workflow.apply_workflow(doc, "action")` instead of setting `doc.status` directly.
*   **Client Side:** The frontend must call the new `get_lhdn_settings` endpoint and adjust the UI based on the `deposit_required` flag and the order's new `Preparing` status.
*   **Deployment:** A `bench migrate` is required to apply the new `deposit_required` field to the database schema.


## Pre-analysis for US-501 (Gemini 2.5 Pro)
Here is the technical brief for implementing user story US-501 in a Frappe/Python environment.

**Subject: Technical Brief for US-501: Conditional Order Approval Flow**

This brief outlines the implementation plan for modifying the order approval process based on a system-wide `depositRequired` setting.

**1. Primary Files to Modify:**
*   `lhdn_payroll_integration/lhdn_payroll_integration/doctype/purchase_order/purchase_order.py`: The controller file for the `Purchase Order` DocType, to modify the `approve` action method.
*   `lhdn_payroll_integration/api.py`: A likely location for creating a new public-facing API endpoint to expose site settings.
*   `lhdn_payroll_integration/www/order_status.py` (and corresponding `.js`/`.html`): The web view files for the customer-facing status page.

**2. Frappe DocTypes & Hooks Involved:**
*   **Purchase Order:** The core DocType. The `status` field will be updated, and its server-side controller method for approval will be the focus.
*   **Site Settings:** The singleton DocType that contains the `depositRequired` boolean field.
*   **Hooks:** A new `@frappe.whitelist(allow_guest=True)` method will be needed to expose settings to unauthenticated users.

**3. Recommended Implementation Approach:**
1.  In `purchase_order.py`, locate the whitelisted approval function (e.g., `approve_order`).
2.  Inside this function, fetch the site settings using `settings = frappe.get_cached_doc('Site Settings')`.
3.  If `settings.deposit_required` is false, set `doc.status = 'To Receive'` and save.
4.  If `settings.deposit_required` is true, maintain the existing logic (likely setting `doc.status = 'To Bill'`).
5.  In `api.py`, create a new whitelisted, guest-allowed function (`get_deposit_setting`) that returns `{'deposit_required': frappe.db.get_single_value('Site Settings', 'deposit_required')}`.
6.  Update the customer-facing order status page's Python and JS to call this new endpoint and conditionally render the payment section.

**4. Edge Cases & Gotchas:**
*   Ensure the `Purchase Order` `status` field includes 'To Receive' as a valid option.
*   Verify permissions on the approval method are restricted to authorized roles.
*   The customer status page must handle the new `'To Receive'` state gracefully, displaying the "kitchen is preparing" message instead of payment instructions.


## Pre-analysis for US-501 (Gemini 2.5 Pro)
Here is a technical brief for implementing the user story within a Frappe/Python context.

**Subject: Technical Brief for US-501: Conditional Order Approval Logic**

This brief outlines the implementation of a conditional approval workflow for an `Order` DocType based on a `Site Settings` flag. The goal is to bypass the payment step for certain orders.

**1. Python Files & DocTypes to Modify:**
*   **Primary Logic File:** `.../doctype/order/order.py`. The core logic will be in the whitelisted method that handles order approval.
*   **Involved DocTypes:**
    *   `Order`: A custom DocType. Its `status` field needs modification.
    *   `Site Settings`: The standard singleton DocType. A custom field `deposit_required` (type: Check, default: 0) must be added.
*   **API File:** A method in `.../doctype/order/order_api.py` (or similar) will be needed to expose the `deposit_required` setting to the front end.

**2. Recommended Implementation Approach:**
1.  In the `order.py` controller, find the `@frappe.whitelist()` decorated method for the "approve" action.
2.  Within this method, retrieve the global setting: `deposit_required = frappe.db.get_single_value('Site Settings', 'deposit_required')`.
3.  Implement an `if/else` branch:
    *   If `not deposit_required`, set `self.status = 'Preparing'` and save the document.
    *   Else (if `deposit_required`), retain the existing logic, likely setting `self.status = 'Approved'`.
4.  Update the customer-facing Web Template (`.html` and `.js` for the order status page) to fetch the setting and conditionally render the payment upload section based on the order's status.

**3. Edge Cases & Gotchas:**
*   The `status` field in the `Order` DocType meta must have "Preparing" added to its list of Options.
*   The API exposing `deposit_required` should be public but read-only.
*   Ensure front-end logic correctly handles the two distinct states ('Preparing' vs. 'Approved') to show the correct messaging and UI elements.


## Pre-analysis for US-501 (Gemini 2.5 Pro)
Here is a technical brief for user story US-501, based on the provided JSON.

**Subject: US-501 Technical Brief: Conditional Order Approval Workflow**

**Core Requirement:** The admin order approval action must conditionally bypass the customer payment step. This is controlled by the `depositRequired` boolean in the site settings.

**Files to Modify:**
*   `src/app/api/admin/orders/[id]/route.ts`
*   `src/app/api/settings/route.ts`
*   `src/app/[locale]/order/[id]/page.tsx`

**Implementation Approach:**

1.  **Admin API (`.../orders/[id]/route.ts`):**
    *   In the `PATCH` handler, inside the `action === 'approve'` block, call `getSiteSettings()`.
    *   If `settings.depositRequired` is `false`, set the order status directly to `'preparing'`.
    *   If `true`, keep the existing logic, setting status to `'approved'`.

2.  **Public Settings API (`.../settings/route.ts`):**
    *   Update the `GET` handler to include the `depositRequired` field in the public-facing response. This exposes the setting to the client.

3.  **Customer Order Page (`.../order/[id]/page.tsx`):**
    *   Fetch both the order data and the site settings (`/api/settings`).
    *   If `!depositRequired` and `order.status === 'preparing'`, hide the payment upload component and display the "Your order is confirmed..." message.

**Gotchas & Edge Cases:**
*   The `estimatedReady` time must still be saved during the `approve` action, even when `depositRequired` is false.
*   The customer order page's rendering logic depends on *both* the `depositRequired` flag and the `order.status`. Ensure both are available before rendering.
*   No database changes are needed; the 'preparing' status is already supported.

## Iteration 1 - Fri, Mar 13, 2026 12:44:28 AM
Incomplete: Wire depositRequired into approve action (ID: US-501) — attempt 1/3


## Pre-analysis for US-501 (Gemini 2.5 Pro)
Here is the technical brief for the Claude agent:

**Subject:** Technical Brief for US-501: Conditional Order Approval Workflow

This user story requires modifying the order approval process to bypass the payment step based on a system setting.

**1. Python Files to Modify:**
*   `lhdn_payroll_integration/api.py`: Create a new whitelisted `approve_order` controller method here. This method will contain the core logic.
*   Also in `api.py`, create a new whitelisted, guest-allowed method `get_payroll_settings` to expose the setting to the front end.

**2. Frappe DocTypes & Hooks:**
*   **Sales Order (or similar DocType):** This is the primary document. The `approve_order` method will update its `status` field.
*   **LHDN Payroll Settings (Singleton DocType):** This DocType must contain the `deposit_required` (Type: Check, Default: 0) field. If this settings DocType doesn't exist, it needs to be created.
*   **No hooks required;** the changes are triggered by a direct API call from the admin UI.

**3. Recommended Implementation:**
1.  In `api.py`, define `@frappe.whitelist() def approve_order(order_id):`.
2.  Inside, fetch the setting: `settings = frappe.get_cached_doc('LHDN Payroll Settings')`.
3.  Fetch the order: `order = frappe.get_doc('Sales Order', order_id)`.
4.  If `settings.deposit_required`: maintain the current logic, setting `order.status = 'Approved'`.
5.  If not `settings.deposit_required`: set `order.status = 'Preparing'`.
6.  Remember to call `order.save()` and check permissions within the method.
7.  The `get_payroll_settings` method should simply return `{'deposit_required': settings.deposit_required}`.

**4. Edge Cases & Gotchas:**
*   **Workflow:** Check if the `Sales Order` DocType has a Document Workflow. If so, directly setting the status might fail. You may need to adjust the workflow transitions to allow this new state change.
*   **Permissions:** Ensure the `approve_order` method validates that the logged-in user has the permission to approve orders.
*   **Client-Side:** The customer-facing portal page needs to call `get_payroll_settings` and conditionally render the payment upload section based on the returned value and order status.


## Pre-analysis for US-501 (Gemini 2.5 Pro)
This technical brief is for a Claude agent tasked with implementing a feature in a Frappe/Python context, based on a provided user story.

**Project:** `lhdn_payroll_integration`
**User Story:** US-501 - Conditional 'approve' action based on a system setting.

**Technical Analysis & Implementation Plan:**

The user story describes a Next.js/TypeScript implementation. This brief translates that logic into the equivalent Frappe/Python architecture.

1.  **Primary Python File:**
    *   A whitelisted API function, likely in a file such as `lhdn_payroll_integration/api.py`, will contain the core logic. This function handles the "approve" action for the relevant DocType.

2.  **Frappe DocTypes & Hooks:**
    *   **DocType (Singleton):** A Singleton DocType, analogous to `SiteSettings`, should be created or used (e.g., `LHDN Payroll Settings`). It must contain a Check field named `deposit_required` (default: 0).
    *   **DocType (Transaction):** A central DocType (e.g., `Payroll Submission`) with a `status` field that includes states like 'Approved', 'Preparing', and 'Pending Payment'.
    *   **Hooks (`hooks.py`):** No specific hooks seem necessary, as the logic is contained within whitelisted API calls.

3.  **Implementation Approach:**
    *   The `approve_submission` function in `api.py` must first read the `deposit_required` setting using `frappe.db.get_single_value('LHDN Payroll Settings', 'deposit_required')`.
    *   Based on the setting, it will set the `status` of the target document to either `Preparing` (if `deposit_required` is false) or `Approved` (if true).
    *   A separate whitelisted function (`get_payroll_settings`) should be created to expose the `deposit_required` value to the frontend, ensuring `allow_guest=True` for public access.

4.  **Edge Cases & Gotchas:**
    *   Ensure the `status` field in the transaction DocType has 'Preparing' as a valid option.
    *   Permissions for the whitelisted API methods must be correctly set for both admin users (for the action) and public/guest users (for reading settings).
    *   The frontend (DocType client script or Page script) must be updated to call the new settings endpoint and conditionally render the UI as described in the user story.


## Pre-analysis for US-504 (Gemini 2.5 Pro)
Here is the technical brief for implementing the real-time notification user story in a Frappe/Python environment.

***

### **Technical Brief: US-504 - Real-Time Order Notifications in Frappe**

**Objective:** Replace client-side polling with a real-time, event-driven system to notify admins of new orders using Frappe's built-in real-time framework (Socket.IO).

**1. Python Files to Modify:**
*   `lhdn_payroll_integration/hooks.py`: To define the DocType event listener that triggers the real-time event.
*   `lhdn_payroll_integration/api.py` (or a new module): To house the Python logic that formats and publishes the event. A fallback polling endpoint can also be defined here.

**2. Frappe DocTypes & Hooks:**
*   **DocType:** `Sales Order` or a similar custom "Order" DocType. The logic will trigger on this document's lifecycle.
*   **Hook:** The `on_submit` document hook is the ideal candidate to ensure the notification is sent only for committed orders.
*   **Function:** `frappe.publish_realtime` is the core Frappe function to push the event from the backend to connected clients.

**3. Recommended Implementation Approach:**
1.  In `hooks.py`, connect the `on_submit` event for the order DocType to a new Python function (e.g., `lhdn_payroll_integration.api.notify_new_order`).
2.  In that Python function, construct a payload with essential order details (`name`, `grand_total`, etc.).
3.  Use `frappe.publish_realtime("new_order", message=payload, user=None, role="System Manager")` to send the event specifically to admin-level users.
4.  Create a client-side JS file (`/public/js/realtime_notifications.js`) and include it in the app's `build.json` to load it in the Desk UI.
5.  In this JS file, use `frappe.realtime.on("new_order", (data) => { ... })` to listen for the event and trigger a UI update (e.g., animate a bell icon, increment a badge).

**4. Edge Cases & Gotchas:**
*   **Permissions:** Target the `publish_realtime` event to a specific role (e.g., `System Manager`) to avoid leaking order data to unauthorized users.
*   **Fallback:** The client-side JS should implement a `setInterval` with `frappe.call` to a simple count-checking API endpoint as a fallback in case the websocket connection fails.
*   **UI State:** The notification badge count should be reset after the admin views the new orders list.
*   **Build Step:** Remember to run `bench build` after adding or modifying the client-side JS file to apply the changes.

## Iteration 1 - Fri, Mar 13, 2026 12:55:31 AM
Completed: Wire depositRequired into approve action (ID: US-501) in 29m


## Pre-analysis for US-504 (Gemini 2.5 Pro)
This is a technical brief for implementing a Server-Sent Events (SSE) endpoint in a Next.js application to provide real-time order notifications to an admin dashboard.

**1. Files to Modify:**
*   **Create:** `src/app/api/admin/orders/stream/route.ts` - The new Edge Runtime SSE endpoint.
*   **Modify:** `src/components/admin/admin-orders-bell.tsx` - The client-side React component that will consume the SSE stream.

**2. Involved DocTypes & Hooks:**
*   **Framework/Runtime:** Next.js App Router, Vercel Edge Runtime.
*   **API:** Server-Sent Events (`ReadableStream`, `EventSource`).
*   **Database:** Neon, using the `@neondatabase/serverless` driver directly.
*   **Authentication:** Manual JWT parsing using the `jose` library.

**3. Implementation Approach:**
*   **Backend (`route.ts`):** Create a `GET` handler that returns a `ReadableStream`. Inside the stream, use `setInterval` to poll the `tray_orders` table every 3 seconds for new orders. On finding new orders, enqueue a `data: {json}\n\n` event. A separate `setInterval` will send a comment (`: ping\n\n`) every 20s as a heartbeat.
*   **Frontend (`admin-orders-bell.tsx`):** Use a `useEffect` hook to establish an `EventSource` connection. An `onmessage` handler will update the UI with new order notifications. An `onerror` handler will manage the reconnection logic.

**4. Edge Cases & Gotchas:**
*   **Edge Compatibility is critical:** The standard DB client in `@src/lib/db.ts` uses a connection pool and is **not** Edge-compatible. You must import and use `neon` from `@neondatabase/serverless` directly in the route handler.
*   **Edge Authentication:** Similarly, standard auth helpers may rely on Node.js APIs. Authentication must be handled by manually parsing the cookie and using `jose.jwtVerify` for JWT validation.
*   **Connection Lifecycle:** The client must close the `EventSource` connection in the `useEffect` cleanup function to prevent memory leaks.
*   **Graceful Fallback:** After 3 failed reconnection attempts, the client must cleanly terminate SSE efforts and revert to the existing 30-second `setInterval` polling mechanism, updating a status indicator in the UI to reflect this state.

## Iteration 1 - Fri, Mar 13, 2026  1:01:12 AM
Incomplete: Admin SSE for real-time new order notifications (ID: US-504) — attempt 1/3

## Iteration 1 - Fri, Mar 13, 2026  1:01:37 AM
Completed: Wire depositRequired into approve action (ID: US-501) in 14m


## Pre-analysis for US-504 (Gemini 2.5 Pro)
### Technical Brief: Admin SSE Order Notifications (US-504)

**Objective:** Replace polling in the admin bell with a real-time Server-Sent Events (SSE) stream for new order notifications, running on Vercel's Edge Runtime.

**1. Files to Modify:**
*   **Create:** `src/app/api/admin/orders/stream/route.ts` (New Edge Route Handler)
*   **Edit:** `src/components/admin/admin-orders-bell.tsx` (React component to consume SSE)

**2. Framework/Library Involvement:**
*   **Framework:** Next.js (App Router) with Edge Runtime
*   **Libraries:**
    *   `@neondatabase/serverless`: For direct, edge-compatible database queries.
    *   `jose`: For manual JWT verification in the edge environment.
    *   React: For frontend component logic (`useEffect`, `useState`, `useRef`).

**3. Implementation Approach:**
*   **Backend (`route.ts`):**
    *   Export `runtime = 'edge'` and `dynamic = 'force-dynamic'`.
    *   In the `GET` handler, return a `Response` with a `ReadableStream`.
    *   Inside the stream, use `setInterval` to poll the `tray_orders` table every 3s. Use `@neondatabase/serverless` directly for the query (do not use the pooled `sql` from `lib/db.ts`).
    *   Manually parse the admin auth cookie and verify the token using `jose.jwtVerify()`.
    *   Stream `new_order` events for new orders and a `: ping` heartbeat every 20s.
*   **Frontend (`admin-orders-bell.tsx`):**
    *   In a `useEffect`, create and manage an `EventSource` instance.
    *   On `new_order` events, update the badge count and trigger UI feedback.
    *   Implement an `onerror` handler with a 3-attempt reconnect (5s delay) before falling back to `setInterval` polling.
    *   Add a state for and render the "Live" / "Polling" status indicator.
    *   Ensure the `EventSource` connection is closed in the `useEffect` cleanup function.

**4. Edge Cases & Gotchas:**
*   **Edge Compatibility:** The biggest constraint. Do not use Node.js-specific APIs or libraries (e.g., the existing `verifyAdminToken` helper or database connection pools).
*   **Connection Lifecycle:** The server-side stream must correctly handle client disconnects to clean up intervals. The client must gracefully close its connection on unmount.
*   **Auth in Edge:** Authentication must be implemented manually using `jose`, as standard NextAuth helpers might rely on Node.js APIs.
*   **Fallback Logic:** The transition from SSE to polling after repeated failures must be clean, preventing duplicate timers or missed states.


## Pre-analysis for US-504 (Gemini 2.5 Pro)
This technical brief is for a Next.js/TypeScript project. The "Frappe/Python" context seems incorrect based on the story's technical notes.

**1. Files to Modify:**
*   **Create:** `src/app/api/admin/orders/stream/route.ts` (Next.js Edge API Route)
*   **Modify:** `src/components/admin/admin-orders-bell.tsx` (React client component)

**2. Key APIs & Hooks:**
*   **Backend:** Next.js Edge Runtime, `ReadableStream` for SSE.
*   **Frontend:** React `useEffect` for lifecycle, `useState` for state (connection status, count), and `useRef` for managing reconnect attempts.
*   **Libraries:** `jose` for JWT auth, `@neondatabase/serverless` for direct DB access.

**3. Implementation Approach:**
*   **API Route (`route.ts`):**
    *   Export `runtime = 'edge'`.
    *   In the `GET` handler, return a `ReadableStream`.
    *   Inside the stream's `start()` function, create a loop that queries Neon DB every 3s for new orders (`status = 'pending_approval'`).
    *   Emit a `new_order` event with order data.
    *   Send a comment (`: ping`) every 20s as a heartbeat.
*   **Client Component (`admin-orders-bell.tsx`):**
    *   In a `useEffect`, create an `EventSource` instance.
    *   Handle the `new_order` event to update the badge count.
    *   Use the `onerror` handler to implement a 5-second reconnect delay, falling back to 30s `setInterval` polling after 3 failed attempts.
    *   Ensure the `EventSource` is closed in the `useEffect` cleanup function.

**4. Gotchas & Edge Cases:**
*   **Edge Compatibility is critical:**
    *   **Database:** Do NOT use the pooled `sql` helper from `src/lib/db.ts`. You must import `neon` from `@neondatabase/serverless` directly, as connection pools don't work in the Edge Runtime.
    *   **Authentication:** Standard Node.js crypto/auth helpers will fail. Parse the JWT cookie manually and verify it using `jose.jwtVerify()`.
*   The heartbeat comment is essential to prevent the serverless function from timing out.
*   The client must manage a state for the connection status ('Live' vs 'Polling') to render the UI indicator.

## Iteration 2 - Fri, Mar 13, 2026  1:16:14 AM
Incomplete: Admin SSE for real-time new order notifications (ID: US-504) — attempt 2/3

## Iteration 2 - Fri, Mar 13, 2026  1:17:36 AM
Completed: Admin SSE for real-time new order notifications (ID: US-504) in 15m


## Pre-analysis for US-601 (Gemini 2.5 Pro)
Here is a technical brief for implementing the POS Dashboard in a Frappe/Python environment.

**Project:** lhdn_payroll_integration
**User Story:** US-601 - Simple POS Dashboard

### Technical Implementation Brief

**1. Python Files to Modify:**
- **`lhdn_payroll_integration/api.py`**: Create new `@frappe.whitelist()` functions here.
    - `get_pos_orders()`: Fetches all orders with statuses `pending_approval`, `preparing`, `approved`, `ready`. Returns JSON for the frontend.
    - `update_order_status(order_name, new_status, reason=None)`: Handles status transitions, including the conditional logic for `depositRequired` and processing rejections.
    - `get_daily_summary()`: Calculates and returns today's total orders and revenue.
- **`lhdn_payroll_integration/hooks.py`**: Add an entry to the `workspace` hook to create a sidebar link on the Desk pointing to the new "POS Dashboard" page.

**2. Frappe DocTypes & Configuration:**
- **New `Page`**: Create a new Page named "POS Dashboard". This will generate the `/app/pos` route and the corresponding `pos.js` and `pos.html` files in the `public` directory.
- **Existing `Order` DocType**: This will be the primary data source. The API functions will query this DocType.
- **New `Print Format`**: Create a server-side Print Format for the `Order` DocType, named "POS Receipt". This will be a simple, clean layout for printing.

**3. Recommended Implementation Approach:**
- The `pos.html` will contain the basic structure for three columns (e.g., `<div id="pending-col">`).
- The main logic will reside in **`lhdn_payroll_integration/public/js/pos.js`**.
- On page load, use `frappe.call` to fetch data from `get_pos_orders()` and `get_daily_summary()`.
- Dynamically render order cards into the appropriate columns using JavaScript.
- Attach click handlers to card buttons that trigger `frappe.call` to `update_order_status()`.
- Use `setInterval` to poll the backend APIs every 10 seconds for auto-refresh.
- The "Print Receipt" button should open the new Print Format view (`/app/print/Order/{order_id}?format=POS-Receipt`).

**4. Edge Cases & Gotchas:**
- **Permissions**: Ensure the "POS Dashboard" page and whitelisted APIs are restricted to appropriate roles (e.g., "System Manager", "Staff").
- **Status Mapping**: Be precise when querying orders to match the Kanban columns: 'Pending' (`pending_approval`), 'Preparing' (`approved`, `preparing`), 'Ready' (`ready`).
- **Data Volume**: The `get_pos_orders` query should be optimized, possibly filtering by creation date (`today`) to avoid performance degradation over time.
- **UI State**: After a status update, ensure the UI is refreshed with the new state from the server to prevent race conditions between users.

## Iteration 1 - Fri, Mar 13, 2026  9:28:21 AM
Incomplete: Built-in Simple POS Dashboard (ID: US-601) — attempt 1/3


## Pre-analysis for US-601 (Gemini 2.5 Pro)
Here is a technical brief for implementing the user story in a Frappe/Python environment.

***

**To:** Claude Code Agent
**From:** Gemini
**Subject:** Technical Brief for US-601: Simple POS Dashboard (Frappe Implementation)

This brief translates the provided Next.js user story into a plan for a Frappe/Python application.

**1. Python Files to Modify:**
*   `[app_name]/public/js/pos_dashboard.js`: Client-side logic for the dashboard page, including data polling and rendering the sales summary.
*   `[app_name]/api.py`: A new or existing file for whitelisted server-side functions (`@frappe.whitelist()`). This will handle fetching aggregated daily sales data and processing order rejections.
*   `[app_name]/config/desktop.py`: To add the new POS Dashboard page to the Frappe Desk for admin access.

**2. Frappe DocTypes & Hooks:**
*   **Primary DocType:** `Order`. This will be the core data model. Its List View should be configured to use the Kanban layout, grouped by the `status` field.
*   **Workflow:** A new `Workflow` for the `Order` DocType is required to manage status transitions (e.g., Pending -> Preparing -> Ready).
*   **Page:** Create a new custom `Page` named "POS Dashboard". This will serve as the main hub and contain the Kanban view and daily summary widgets.
*   **Print Format:** A new server-side `Print Format` for the `Order` DocType to create the simple, printable receipt.

**3. Recommended Implementation Approach:**
1.  Configure the `Order` DocType's List View to use the built-in Kanban board, mapping columns to the `status` field.
2.  Implement the daily summary by creating a whitelisted Python function that runs a SQL query to aggregate today's orders.
3.  Fetch and display this summary on the "POS Dashboard" `Page` using JS (`frappe.call`). Set it to poll every 10-15 seconds.
4.  Use Frappe's standard `Workflow` system to handle the one-tap status transitions directly from the Kanban view.
5.  Create a "Receipt" `Print Format` and add a custom button to the `Order` form to trigger it.

**4. Gotchas & Edge Cases:**
*   **Permissions:** Double-check that the target user Role has permissions for the `Order` DocType, the `Page`, and the required `Workflow` transitions.
*   **Kanban State Mapping:** The story requires grouping multiple statuses (`approved`, `preparing`) into a single 'Preparing' column. This may require customizing the Kanban board's generation logic.
*   **Performance:** The daily summary query on the `Order` table must be efficient. Ensure `creation` and `status` fields are indexed.
*   **Authentication:** Frappe's standard session management for Pages and DocTypes will handle authentication automatically. No custom JWT logic is needed.

